function smallestOfThreeNumbers(firstNumber, secondNumber, lastNumber) {

    let smallInteger = Math.min(firstNumber, secondNumber, lastNumber);
    console.log(smallInteger);
}
smallestOfThreeNumbers(2, 5, 3);